name: Test

on:
  workflow_call:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    container: oven/bun:1
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/build
      - run: bun run lint
      - run: bun test
